{
  "name": "evalla",
  "version": "0.1.0",
  "description": "Safe math evaluator with variables, dependencies, and precision",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/test/*.test.js",
    "test:watch": "tsc -p tsconfig.test.json && node --test --watch dist-test/test/*.test.js"
  },
  "keywords": [
    "math",
    "evaluator",
    "safe",
    "decimal",
    "precision"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "acorn": "^8.11.3",
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.5",
    "typescript": "^5.3.3"
  }
}
