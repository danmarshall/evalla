{
  "name": "evalla-monorepo",
  "version": "0.1.1",
  "private": true,
  "description": "Evalla monorepo with core library and playground",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "build:evalla": "npm run build --workspace=packages/evalla",
    "test": "npm run test --workspaces",
    "dev:playground": "npm run build:evalla && npm run dev --workspace=packages/playground"
  },
  "overrides": {
    "lodash": "^4.17.23"
  },
  "author": "Dan Marshall",
  "license": "MIT"
}
