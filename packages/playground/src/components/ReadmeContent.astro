---
import { getEntry } from 'astro:content';

const entry = await getEntry('docs', 'readme');
if (!entry) throw new Error('README not found');
const { Content } = await entry.render();
---

<div class="content">
  <Content />
</div>
